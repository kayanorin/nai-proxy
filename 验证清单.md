# 验证清单

三层验证。**逻辑我（Claude）本地用假 NAI 全验过；唯一只有你能测的是「真连 NAI」那段（要真 key）。**

---

## 第 1 层 · 逻辑（已通过，免 key）

`npm install && node test/smoke.js` —— 拉起假 NAI + 代理，断言全部逻辑路径。

当前结果：**11 / 11 全通过**

- [x] 健康检查 + CORS 预检（OPTIONS → 204，允许 `X-Access-Token`）
- [x] 无令牌 / 错令牌 → 401
- [x] 提交 → 轮询 → 取 zip（zip 里有 PNG，张数 = n_samples）
- [x] 任务绑定令牌：他人令牌 → 403
- [x] 串行：任意时刻最多 1 个请求在飞
- [x] 限速：相邻请求起点间隔 ≥ MIN_GAP_MS
- [x] 429 退避重试后成功
- [x] 401 → 任务 failed + code=KEY_INVALID（车主换 key 提示）
- [x] 未完成取结果 → 409
- [x] 取消排队中的任务
- [x] TTL 回收：超时后 job 404

> 改了服务端逻辑后重跑一遍即可。

---

## 第 2 层 · 真 key 冒烟（你来，要真 key）

目的：确认 NAI 真的接受透传的 body、真的回 zip。墙内本机连不上 NAI 的话，**跳过本层，直接做第 3 层**（在 Render 上做）。

```bash
cd nai-proxy
npm install
NAI_KEY=你的真key ACCESS_TOKENS=tok1 node server.js
```

- [ ] 网页设置里「分发服务地址」填 `http://localhost:3000`，「访问令牌」填 `tok1`
- [ ] 生成一张图，能正常出图（说明 NAI 接受 body + zip 回传链路通）
- [ ] 故意把 `NAI_KEY` 改错重启 → 生图任务 failed，提示「车主请更新 NAI_KEY」
- [ ] 看终端日志：请求确实串行、相邻间隔约 `MIN_GAP_MS`

---

## 第 3 层 · Render 出口 IP（你来，部署后）

目的：确认线上能连 NAI、出口 IP 稳定在同一网段、多人并发不触发 429。

```bash
BASE=https://your-app.onrender.com
curl $BASE/                # {ok:true,...}
curl $BASE/egress-ip       # 记下 IP，隔几分钟/冷启动后再查一次对比
```

- [ ] `GET /` 正常（注意首次有冷启动，等几十秒）
- [ ] `GET /egress-ip` 多次（含一次冷启动后）出口 IP 在**同一网段**（同 /24 即可，免费档同区共享）
- [ ] 网页指向 Render 地址 + 令牌，真 key 生一张成功
- [ ] 拉一两个朋友**同时**各点几张：观察任务串行排队、不再频繁 429
- [ ] 给每个朋友发各自的访问令牌（`ACCESS_TOKENS` 逗号分隔），吊销谁就从列表里删谁、重部署

---

## 排错速查

| 现象 | 多半是 |
|---|---|
| 任务一直 401 / BAD_TOKEN | 令牌没填或填错；检查 `X-Access-Token` 与 `ACCESS_TOKENS` |
| 503 NO_KEY / NO_TOKENS | Render 没配 `NAI_KEY` / `ACCESS_TOKENS` |
| 任务 failed，code=KEY_INVALID | NAI key 失效/过期，车主更新 `NAI_KEY` 环境变量 |
| 任务 failed，code=INSUFFICIENT_FUNDS | Anlas 余额不足 |
| 轮询 status 突然 404 | 服务冷启动过、内存清空 → 客户端重新提交 |
| 频繁 429 仍出现 | 调大 `MIN_GAP_MS`（如 15000） |
